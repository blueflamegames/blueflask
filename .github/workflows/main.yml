name: "Test"

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Run pylint
      run: |
        pip3 install -r requirements.txt
        pip3 install setuptools pylint
        pylint bluflame.py
        echo "Create tests to run here"
